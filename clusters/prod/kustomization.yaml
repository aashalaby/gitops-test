apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Everything the root Application pulls in. Adding a new cluster means
# copying this directory and changing the destination server + values.
namespace: argocd

resources:
  - projects.yaml
  - argocd.yaml
  - platform-appset.yaml
  - apps-appset.yaml
